import 'package:flutter/material.dart';

import '../model/sale.dart';

class StandardSaleCard extends StatelessWidget {
  final Sale? sale;

  StandardSaleCard({this.sale});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
